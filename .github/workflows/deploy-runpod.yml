name: 🚀 CI/CD - Deploy GPU Agent to RunPod (REST API + CLI for Pod Launch)

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy-gpu:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v3

      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: 🔐 Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USER }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: 🧱 Build & Push GPU Docker Image
        run: |
          docker build -f docker/Dockerfile.gpu -t ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest .
          docker push ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest

      - name: 🛠 Install runpodctl CLI
        run: |
          wget -q https://github.com/Run-Pod/runpodctl/releases/download/v1.14.3/runpodctl-linux-amd64 -O runpodctl
          chmod +x runpodctl
          sudo mv runpodctl /usr/bin/runpodctl
          runpodctl config --apiKey ${{ secrets.RUNPOD_API_KEY }}

      - name: 🚀 Launch GPU Pod from Template (CLI)
        run: |
          UNIQUE_POD_NAME="github-actions-pod-${{ github.run_id }}"
          echo "Launching Pod: $UNIQUE_POD_NAME"
          POD_ID=$(runpodctl create pod \
            --templateId uhqe2m9jvy \
            --name "$UNIQUE_POD_NAME" \
            --gpuType "NVIDIA GeForce RTX 3090" \
            --gpuCount 1 \
            --imageName "ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest" \
            --ports "8080/http" \
            --volumePath "/runpod" \
            --containerDiskSize 20 \
            --volumeSize 1 \
            --mem 30 \
            --vcpu 8 \
            --cost 0.22 | grep -oP '(?<=pod ")(.*?)(?=")')
          echo "RUNPOD_POD_ID=$POD_ID" >> $GITHUB_ENV
          echo "✅ Pod ID: $POD_ID"

      - name: 🔄 Poll Pod Status via REST API
        id: poll_status
        run: |
          echo "Polling pod status..."
          for i in {1..30}; do
            sleep 10
            response=$(curl -s -X GET https://api.runpod.io/v1/pods/$RUNPOD_POD_ID \
              -H "Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}")

            echo "📡 Response: $response"

            STATUS=$(echo "$response" | jq -r '.status')
            echo "⏳ Pod status: $STATUS"

            if [ "$STATUS" = "RUNNING" ]; then
              PUBLIC_IP=$(echo "$response" | jq -r '.publicIp')
              if [ -n "$PUBLIC_IP" ] && [ "$PUBLIC_IP" != "null" ]; then
                echo "✅ Pod is live at: http://$PUBLIC_IP:8080"
                echo "RUNPOD_URL=http://$PUBLIC_IP:8080" >> $GITHUB_ENV
                echo "RUNPOD_URL=http://$PUBLIC_IP:8080" >> $GITHUB_OUTPUT
                break
              fi
            fi
          done

          if [ -z "$PUBLIC_IP" ] || [ "$PUBLIC_IP" = "null" ]; then
            echo "❌ Pod failed to start or no public IP assigned."
            exit 1
          fi

      - name: 🔐 Store Pod URL as GitHub Secret
        run: gh secret set RUNPOD_URL --body "${{ steps.poll_status.outputs.RUNPOD_URL }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}