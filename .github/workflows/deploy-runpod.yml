name: 🚀 CI/CD - Deploy GPU Agent to RunPod (runpodctl + REST)

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy-gpu:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repo
        uses: actions/checkout@v3

      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: 🔐 Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USER }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: 🧱 Build & Push GPU Docker Image
        run: |
          docker build -f docker/Dockerfile.gpu -t ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest .
          docker push ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest

      - name: 🛠 Install runpodctl CLI
        run: |
          wget -q https://github.com/Run-Pod/runpodctl/releases/download/v1.14.3/runpodctl-linux-amd64 -O runpodctl
          chmod +x runpodctl
          sudo mv runpodctl /usr/bin/runpodctl

      - name: 🔐 Authenticate with RunPod
        run: runpodctl config --apiKey ${{ secrets.RUNPOD_API_KEY }}

      - name: 🚀 Launch GPU Pod from Template
        id: launch_pod
        run: |
          UNIQUE_POD_NAME="github-actions-pod-${{ github.run_id }}"
          echo "Launching Pod: $UNIQUE_POD_NAME"
          POD_ID=$(runpodctl create pod \
            --templateId uhqe2m9jvy \
            --name "$UNIQUE_POD_NAME" \
            --gpuType "NVIDIA GeForce RTX 3090" \
            --gpuCount 1 \
            --imageName "ghcr.io/${{ secrets.GHCRL_USER }}/autonomind-gpu:latest" \
            --ports "8080/http" \
            --volumePath "/runpod" \
            --containerDiskSize 20 \
            --volumeSize 1 \
            --mem 30 \
            --vcpu 8 \
            --cost 0.22 | grep -oP '(?<=pod ")(.*?)(?=")')
          echo "RUNPOD_POD_ID=$POD_ID" >> $GITHUB_ENV
          echo "✅ Pod ID: $POD_ID"

      - name: 🔄 Poll Pod Status via REST API
        id: poll
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
          RUNPOD_POD_ID: ${{ env.RUNPOD_POD_ID }}
        run: |
          echo "Polling Pod status from REST API..."
          for i in {1..30}; do
            STATUS_JSON=$(curl -s -X GET https://api.runpod.ai/v1/pods/$RUNPOD_POD_ID \
              -H "Authorization: Bearer $RUNPOD_API_KEY" \
              -H "Content-Type: application/json")
            
            STATUS=$(echo "$STATUS_JSON" | jq -r '.status')
            PUBLIC_IP=$(echo "$STATUS_JSON" | jq -r '.publicIp')

            echo "⏳ Status: $STATUS | IP: $PUBLIC_IP"

            if [ "$STATUS" = "RUNNING" ] && [ -n "$PUBLIC_IP" ] && [ "$PUBLIC_IP" != "null" ]; then
              echo "✅ Pod is live: http://$PUBLIC_IP:8080"
              echo "POD_URL=http://$PUBLIC_IP:8080" >> $GITHUB_ENV
              break
            fi
            sleep 10
          done

          if [ -z "$PUBLIC_IP" ] || [ "$PUBLIC_IP" = "null" ]; then
            echo "❌ Pod failed to start or no public IP assigned."
            exit 1
          fi

      - name: 🔐 Store Pod URL as GitHub Secret
        run: gh secret set RUNPOD_URL --body "$POD_URL"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          POD_URL: ${{ env.POD_URL }}
